# Fredrick Inspired Trading Bot

- Use Python 3.11+ and the `src` layout.
- Keep Alpaca credentials in environment variables; never commit `.env`.
- Preserve paper-trading and dry-run safety defaults.
- Keep broker calls behind the broker abstraction and test them with mocks.
- Run `pytest` and `ruff check .` after implementation changes.
